import React from 'react'
import { StyleSheet, Text, View, ScrollView } from 'react-native'

const AboutScreen = ({ navigation }) => {
    return (
        <View>
            <ScrollView>
                <Text>Welcome to Spin</Text>
            </ScrollView>
        </View>
    )
}

export default AboutScreen

const styles = StyleSheet.create({})
